# 1. DB 연결
spring.datasource.url=jdbc:mariadb://localhost:3306/kpbridge_db?serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=tiger

# 2. JPA/Hibernate 설정 (엔티티를 보고 테이블 상태를 유지)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# 3. 보안 및 인코딩 (POST 데이터 전송 시 한글 깨짐 방지)
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true

# 터미널에 자잘한 정보 안 뜨게 하기
logging.level.root=INFO
server.port=8080

spring.thymeleaf.cache=false

# i18n 메시지 소스
spring.messages.basename=messages
spring.messages.encoding=UTF-8
spring.messages.cache-duration=3600

# Caffeine 캐시 설정
spring.cache.type=caffeine
spring.cache.caffeine.spec=maximumSize=100,expireAfterWrite=5s